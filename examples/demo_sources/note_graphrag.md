# GraphRAG Note

GraphRAG matters for SnapGraph because vague recall questions often depend on relationships between sources, thoughts, projects, and open loops.

The first version should not copy a full Microsoft GraphRAG pipeline. It should use lightweight graph paths that explain why a source was saved.

Open loop: test whether graph paths make answers feel different from keyword search.

