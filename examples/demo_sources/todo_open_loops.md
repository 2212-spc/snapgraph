# SnapGraph Open Loops

Todo: build Graph Wiki diagnostics before building the Streamlit UI.

Todo: verify that every graph edge has evidence_source_id.

Todo: create an ask command only after graph paths can be inspected.

