# On-Device Model Note

端侧模型是 SnapGraph 的未来部署形态，不是 v0.1 的核心价值验证。

The first milestone should prove that cognitive context and graph recall work before depending on on-device multimodal inference.

Open loop: define which parts of the pipeline must run locally in a future version.

