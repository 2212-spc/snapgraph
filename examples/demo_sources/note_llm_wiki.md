# LLM Wiki Note

LLM Wiki is useful because raw sources remain immutable while the generated wiki becomes a durable working memory.

The key design is not to answer every question directly from raw documents, but to maintain index.md, log.md, and curated wiki pages over time.

Open loop: decide which parts of the LLM Wiki pattern must stay visible in the SnapGraph demo.

